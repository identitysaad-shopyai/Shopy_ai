# Force all package installs (npm AND yarn) to use the public npm registry.
# This prevents Replit's internal package-firewall proxy URLs from leaking
# into lockfiles, which would cause EAS Build cloud servers to fail because
# they cannot reach http://package-firewall.replit.local
registry=https://registry.npmjs.org
